# Phase 1 - Structures

## ht_item

Think of ht_item as a box.

The box has two compartments:

- key
- value

The box does not store the actual string.
It stores the address (pointer) of the string.

Example:

key  -> "apple"
value -> "100"